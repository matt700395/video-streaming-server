syntax = "proto3";

package opencv;

service StreamService {
    rpc SendStream (stream OcvMat) returns (google.protobuf.Empty) {}
}

message OcvMat {
    int32 rows = 1;
    int32 cols = 2;
    int32 elt_type = 3;
    int32 elt_size = 4;
    bytes mat_data = 5;
}